import React from "react";
import { ReactNode } from "react";
import { ViewInfo } from "../@types/view";
export declare function Scrollable({ children, viewInfo, }: {
    children: ReactNode;
    viewInfo: ViewInfo;
}): React.JSX.Element;
